import userList from './UserList.module.scss';

const UserList = () => {
    return (
        <div className={`${userList.main}`}>
            user list
        </div>
    );
};

export default UserList;